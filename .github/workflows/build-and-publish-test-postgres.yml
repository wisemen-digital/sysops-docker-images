---
name: Build & Publish Test Postgres with optional TimescaleDB & PostGIS

on:
  pull_request:
    paths: &watch_paths
      - '.github/actions/**'
      - '.github/workflows/build-and-publish-test-postgres.yml'
      - '.github/workflows/internal-build-test-and-publish.yml'
      - 'test-postgres/**'
  push:
    branches:
      - main
    paths: *watch_paths
  schedule:
    - cron: "0 0 1,15 * *"  # Every 2 weeks
  workflow_dispatch:

jobs:
  build-test-and-publish:
    uses: ./.github/workflows/internal-build-test-and-publish.yml
    strategy:
      matrix:
        version:
          - alpine: '3.24'
            postgis: '3.6.3'
            postgis_sha256: '58ff19ae133e470280efb4949ef92e0364d4c2a66bef8c57e69477348d815ea3'
            postgres: '16'
            timescaledb: '2.29.2'
            timescaledb_sha256: 'e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855'
          - alpine: '3.24'
            postgis: '3.6.3'
            postgis_sha256: '58ff19ae133e470280efb4949ef92e0364d4c2a66bef8c57e69477348d815ea3'
            postgres: '17'
            timescaledb: '2.29.2'
            timescaledb_sha256: 'e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855'
          - alpine: '3.24'
            postgis: '3.6.3'
            postgis_sha256: '58ff19ae133e470280efb4949ef92e0364d4c2a66bef8c57e69477348d815ea3'
            postgres: '18'
            timescaledb: '2.29.2'
            timescaledb_sha256: 'e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855'
        target:
          - name: bare
            suffix:
          - name: postgis
            suffix: -postgis
          - name: postgis-timescaledb
            suffix: -postgis-timescaledb
    with:
      image: test-postgres
      version: ${{ matrix.version.postgres }}${{ matrix.target.suffix }}
      target: ${{ matrix.target.name }}
      run-tests: false
      build-args: |
        ALPINE_VERSION=${{ matrix.version.alpine }}
        POSTGIS_SHA256=${{ matrix.version.postgis_sha256 }}
        POSTGIS_VERSION=${{ matrix.version.postgis }}
        POSTGRES_VERSION=${{ matrix.version.postgres }}
        TIMESCALEDB_SHA256=${{ matrix.version.timescaledb_sha256 }}
        TIMESCALEDB_VERSION=${{ matrix.version.timescaledb }}
